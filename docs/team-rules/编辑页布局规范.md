# 编辑页布局规范

| 项 | 内容 |
|----|------|
| **规则名称** | 编辑页布局规范 |
| **规则制定人** | 征征 |
| **制定日期** | 2026-07-27 |
| **适用范围** | B 端编辑页 / 配置页（新增、编辑、规则配置、说明编辑等） |
| **状态** | 生效 |

> 制定人姓名若需更正，直接改本表「规则制定人」，并同步更新 `README.md` 索引。

## 规则说明

后续新增或改造编辑页 / 配置页，**除非另有说明**，一律遵循：

1. **内容区自适应**：配置内容区域 `width: 100%`，不要写死较窄的 `max-width`（如 760 / 820 / 960 / 1100）；字段控件随屏宽展开。
2. **底栏固定悬浮**：取消 / 保存 / 重置等操作按钮固定在视口底部（侧栏展开收起时同步调整位置）；内容区预留底部内边距，避免被底栏遮挡。
3. **复用公共样式**：引入 `css/lf-mdm-edit-page.css`；`main.main-content` 增加 class `mdm-edit-page`；底栏使用已有 footer class，或统一加 `mdm-edit-page__footer`。

## 例外

仅当明确要求「弹窗编辑」「窄版表单」「底栏随内容滚动」等时，可不按本规范。

## 参考落地

- 编辑积分商品：`MDM/mdm_marketing_points_mall_form.html`
- 新增/编辑等级：`MDM/mdm_member_level_form.html`
- 积分规则：`MDM/mdm_member_points_rule.html`
- 消费送积分：`MDM/mdm_member_points_consume_form.html`
- 积分抵现：`MDM/mdm_member_points_cash_form.html`
- 积分规则说明：`MDM/mdm_marketing_points_rule_desc.html`
- 批量打标签编辑：`MDM/mdm_member_batch_tag_form.html`

## Agent 镜像

Cursor 自动应用：`.cursor/rules/mdm-edit-page-layout.mdc`
