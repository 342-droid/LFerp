---
description: C 端多状态演示统一用悬浮「xxx验收开关」面板（对齐注册有礼/黑名单）
alwaysApply: true
---

# C 端多状态验收开关规范

> 团队规则权威文档：`docs/team-rules/C端多状态验收开关规范.md`（含规则名称、制定人）

后续在 **用户 APP（`user-app/`）** 新增或改造需切换多种业务状态验收的能力时，除非用户另有说明，一律提供悬浮验收开关面板。

## 必须

1. 固定角悬浮小卡片，标题为「xxx验收开关」
2. 用下拉/勾选表达状态；橙色按钮「应用并刷新」写入 localStorage 后刷新生效
3. 视觉对齐既有 `ua-rg-demo`（白底、浅边、圆角、轻阴影、主色 `#ff7019`）
4. 同页多面板错开位置（默认右下；冲突则左下或上移）
5. 页面被业务态锁死时，验收面板仍可操作以便切回

## 参考

- 注册有礼：`user-app/js/user-app-register-gift.js`、`.ua-rg-demo`
- 黑名单：`user-app/js/user-app-blacklist-guard.js`、`.ua-bl-demo`

## 例外

- 一次性单参数调试可不做面板；会反复多状态验收的能力必须补面板
- B 端配置页不套用本 C 端面板
