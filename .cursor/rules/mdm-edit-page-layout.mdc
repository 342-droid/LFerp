---
description: MDM/营销等 B 端「编辑/配置」页布局规范：内容区随屏宽自适应，底栏固定悬浮
alwaysApply: true
---

# 编辑页布局规范

> 团队规则权威文档：`docs/team-rules/编辑页布局规范.md`（含规则名称、制定人）

后续新增或改造 **编辑页 / 配置页**（含新增、编辑、规则配置、说明编辑等），除非用户另有说明，一律遵循：

## 必须

1. **内容区自适应**：配置内容区域 `width: 100%`，不要写死较窄的 `max-width`（如 760/820/960/1100）；字段控件可随屏宽展开。
2. **底栏固定悬浮**：取消 / 保存 / 重置等操作按钮放在页面底部操作栏，使用 `position: fixed` 贴视口底部（随侧栏展开收起调整 `left`），内容区预留底部内边距避免遮挡。
3. **复用公共样式**：引入 `css/lf-mdm-edit-page.css`；`main.main-content` 增加 class `mdm-edit-page`；底栏使用已有 footer class，或统一加 `mdm-edit-page__footer`。

## 参考已落地页面

- 编辑积分商品：`MDM/mdm_marketing_points_mall_form.html`
- 新增/编辑等级：`MDM/mdm_member_level_form.html`
- 积分规则：`MDM/mdm_member_points_rule.html`
- 积分规则说明：`MDM/mdm_marketing_points_rule_desc.html`
- 批量打标签编辑：`MDM/mdm_member_batch_tag_form.html`

## 例外

仅当用户明确要求「弹窗编辑」「窄版表单」「底栏随内容滚动」等时，才可不按本规范。
