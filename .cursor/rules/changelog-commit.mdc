---
description: 提交信息规范——commit body 按页面写人话说明，自动进入站点更新日志
alwaysApply: true
---

# 提交信息规范（更新日志数据源）

> 团队规则权威文档：`docs/team-rules/提交信息与更新日志规范.md`（含规则名称、制定人）

本仓库的更新日志页（changelog.html）直接由 git 提交生成：标题作为整体概述展示，
body 里的按页面说明会挂到对应页面的更新明细里。每次提交必须按以下格式写 commit message。

## 标题（必须）

`<type>: <一句业务同事能看懂的中文概述>`

- type 用 feat / fix / refactor / style / docs / chore
- 概述写「做了什么事」，不写技术实现（❌「重构 order-live-list.js」 ✅「零售订单支持批量上传快递单」）

## body（改动涉及页面时必须）

每个受影响的页面写一行，格式固定：

`- <页面中文名>: <这个页面上发生了什么变化，一句人话>`

- 页面中文名用该页面 `<title>` 里「 - 」后面的名字（如「售后单」「零售订单」「订单详情 · 用户 APP」）
- 说明面向看原型的产品/业务同事：写界面、交互、流程的变化，不写代码细节
- **弹窗、抽屉、tab 里的变化必须写**——整页截图对比拍不到这些层，这行字是业务同事唯一能得知变化的渠道
- 与具体页面无关的改动（全局样式、构建、文档）写成 `- 通用: <说明>`，或省略
- 每行一句话说完，不换行续写；同一页面多处变化用「；」连接
- 优先引用你在代码里写的中文业务注释（状态矩阵、展示规则、枚举说明等）——那是最准的素材，
  提炼成一句话放进对应页面的说明里

## 示例

```
feat: 完善售后状态流转并调整零售批量快递单维度

- 售后单: 售后状态新增「待收货」，列表徽章随流转更新
- 售后详情: 拒收退回的处理动作补进抽屉，支持填写退回物流单号
- 零售订单: 批量上传快递单改为按包裹维度，一单多包裹用逗号分隔
```
